# PeerJS Server Configuration
# Set to 'true' to use local PeerJS server (localhost:9000)
# Set to 'false' or remove to use cloud PeerJS server (0.peerjs.com)
# For local testing, run: npm run peerserver (in a separate terminal)
VITE_USE_LOCAL_PEERSERVER=false
